import { Client } from 'discord.js';
import { PinguGuild } from '.';

export function GetPClient(client: Client, pGuild: PinguGuild) {
    return pGuild.clients.find(c => c && c._id == client.user.id);
}