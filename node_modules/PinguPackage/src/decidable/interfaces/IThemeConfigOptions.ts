import IDecidableConfigOptions from './IDecidableConfigOptions';
import { PRole } from '../../database';
import { Theme } from '../items';

export default interface IThemeConfigOptions extends IDecidableConfigOptions {
    allowSameWinner: boolean;
    ignoreLastWins: number;
    hostRole: PRole;
    winnerRole: PRole;
    themes: Theme[]
}