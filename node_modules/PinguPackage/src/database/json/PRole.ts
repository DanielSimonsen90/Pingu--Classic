import { Role } from 'discord.js';
import PItem from './PItem';

export default class PRole extends PItem {
    constructor(role: Role) {
        try { super(role) }
        catch { return undefined; }
    }
}