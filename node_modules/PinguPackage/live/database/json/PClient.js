"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const PinguLibrary_1 = require("../../pingu/library/PinguLibrary");
const config = require("../../config.json");
const { Prefix, BetaPrefix } = config;
class PClient {
    constructor(client, guild) {
        this._id = client.user.id;
        this.displayName = guild.me.displayName;
        let clientIndex = guild.client.user.id == PinguLibrary_1.PinguLibrary.Clients.PinguID ? 0 : 1;
        this.embedColor = guild.me.roles.cache.find(role => role.managed).color || PinguLibrary_1.PinguLibrary.DefaultEmbedColor;
        this.prefix = clientIndex == 1 ? BetaPrefix : Prefix;
    }
}
exports.default = PClient;
