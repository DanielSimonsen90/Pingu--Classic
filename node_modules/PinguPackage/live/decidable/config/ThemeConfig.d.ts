import { IThemeConfigOptions } from '../interfaces';
import { PRole, PChannel } from '../../database';
import { Theme } from '../items';
export default class ThemeConfig implements IThemeConfigOptions {
    constructor(options?: IThemeConfigOptions);
    allowSameWinner: boolean;
    ignoreLastWins: number;
    hostRole: PRole;
    winnerRole: PRole;
    themes: Theme[];
    channel: PChannel;
    firstTimeExecuted: boolean;
}
