import { IGiveawayConfigOptions } from '../interfaces';
import { PRole, PChannel } from '../../database';
import { Giveaway } from '../items';
export default class GiveawayConfig implements IGiveawayConfigOptions {
    constructor(options?: IGiveawayConfigOptions);
    allowSameWinner: boolean;
    hostRole: PRole;
    winnerRole: PRole;
    giveaways: Giveaway[];
    channel: PChannel;
    firstTimeExecuted: boolean;
}
