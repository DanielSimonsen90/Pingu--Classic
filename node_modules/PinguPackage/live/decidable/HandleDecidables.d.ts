import { Message } from 'discord.js';
import { PClient } from '../database/index';
import { PinguGuild } from '../pingu/guild/PinguGuild';
import { GiveawayConfig, PollConfig, SuggestionConfig } from '../decidable/config';
declare enum DecidablesTypes {
    Giveaway = "Giveaway",
    Poll = "Poll",
    Suggestion = "Suggestion"
}
interface DecidablesParams {
    message: Message;
    args: string[];
    pGuild: PinguGuild;
    pGuildClient: PClient;
    decidablesType: DecidablesTypes;
    reactionEmojis: string[];
    listEmojis: string[];
    config: GiveawayConfig | PollConfig | SuggestionConfig;
}
declare function HandleDecidables(params: DecidablesParams): Promise<void | Message>;
export { HandleDecidables, DecidablesTypes };
