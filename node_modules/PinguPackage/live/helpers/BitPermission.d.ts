import { PermissionString } from 'discord.js';
export default class BitPermission {
    constructor(permString: PermissionString | string, bit: number);
    permString: PermissionString | string;
    bit: number;
}
