import { Client, Guild, GuildMember } from 'discord.js';
import { PItem, PClient, PGuildMember, PChannel } from "../../database/json";
import { GiveawayConfig, PollConfig, SuggestionConfig, ThemeConfig } from '../../decidable/config';
import { ReactionRole } from './items';
declare class PinguGuild extends PItem {
    static WritePGuild(client: Client, guild: Guild, scriptName: string, succMsg: string, errMsg: string): Promise<void>;
    static GetPGuild(guild: Guild): Promise<PinguGuild>;
    static UpdatePGuild(client: Client, updatedProperty: object, pGuild: PinguGuild, scriptName: string, succMsg: string, errMsg: string): Promise<void>;
    static DeletePGuild(client: Client, guild: Guild, scriptName: string, succMsg: string, errMsg: string): Promise<PinguGuild>;
    static GetPGuilds(): Promise<PinguGuild[]>;
    static GetPClient(client: Client, pGuild: PinguGuild): PClient;
    constructor(guild: Guild, owner?: GuildMember);
    guildOwner: PGuildMember;
    clients: PClient[];
    welcomeChannel: PChannel;
    reactionRoles: ReactionRole[];
    giveawayConfig: GiveawayConfig;
    pollConfig: PollConfig;
    suggestionConfig: SuggestionConfig;
    themeConfig: ThemeConfig;
}
export { PinguGuild };
