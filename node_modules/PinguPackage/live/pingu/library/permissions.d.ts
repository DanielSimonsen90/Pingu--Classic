import { User, GuildChannel, Client } from "discord.js";
import { BitPermission } from "../../helpers";
export declare function PermissionCheck(check: {
    author: User;
    channel: GuildChannel;
    client: Client;
    content: string;
}, permissions: string[]): string;
export declare var PermissionGranted: string;
export declare function Permissions(): {
    given: BitPermission[];
    missing: BitPermission[];
    all: BitPermission[];
};
