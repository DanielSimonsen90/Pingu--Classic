import { Client, User } from "discord.js";
export declare function Developers(client: Client): {
    Danho(): User;
    SynthySytro(): User;
    Slohtman(): User;
    DefilerOfCats(): User;
    includes(id: string): boolean;
};
export declare function isPinguDev(user: User): boolean;
