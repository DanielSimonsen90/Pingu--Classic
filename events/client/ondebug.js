const { PinguLibrary, PinguEvent } = require("PinguPackage");

//module.exports = new PinguEvent('debug', null,
//    async function execute(client, value) {
//        return PinguLibrary.consoleLog(client, "**Debug:**\n```" + `${value}\n` + "```");
//    }
//);