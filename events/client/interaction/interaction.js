const { PinguEvent } = require('PinguPackage');

module.exports = new PinguEvent('interaction',
    async function setContent(client, embed, interaction) {
        
    },
    async function execute(client, interaction) {
        
    }
);