const { Client } = require("discord.js");

module.exports = {
    name: 'events: shardResume',
    /**@param {Client} client
     @param {{id: number, replayedEvents: number}}*/
    execute(client, { id, replayedEvents }) {

    }
}