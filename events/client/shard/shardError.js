const { Client } = require("discord.js");

module.exports = {
    name: 'events: shardError',
    /**@param {Client} client
     @param {{err: Error, id: number}}*/
    execute(client, { err, id }) {

    }
}