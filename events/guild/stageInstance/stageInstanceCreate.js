const { PinguEvent } = require('PinguPackage');

module.exports = new PinguEvent('stageInstanceCreate',
    async function setContent(client, embed, stage) {
        
    },
    async function execute(client, stage) {
        
    }
);