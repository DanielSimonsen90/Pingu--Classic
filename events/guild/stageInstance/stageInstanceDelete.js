const { PinguEvent } = require('PinguPackage');

module.exports = new PinguEvent('stageInstanceDelete',
    async function setContent(client, embed, instance) {
        
    },
    async function execute(client, instance) {
        
    }
);