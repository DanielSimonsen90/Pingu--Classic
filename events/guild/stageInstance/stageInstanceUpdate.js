const { PinguEvent } = require('PinguPackage');

module.exports = new PinguEvent('stageInstanceUpdate',
    async function setContent(client, embed, previous, current) {
        
    },
    async function execute(client, previous, current) {
        
    }
);