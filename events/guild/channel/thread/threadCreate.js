const { PinguEvent } = require('PinguPackage');

module.exports = new PinguEvent('threadCreate',
    async function setContent(client, embed, thread) {
        
    },
    async function execute(client, thread) {
        
    }
);