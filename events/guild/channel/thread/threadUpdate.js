const { PinguEvent } = require('PinguPackage');

module.exports = new PinguEvent('threadUpdate',
    async function setContent(client, embed, preChannel, channel) {
        
    },
    async function execute(client, preChannel, channel) {
        
    }
);