const { Client, Invite } = require("discord.js");

module.exports = {
    name: 'events: inviteCreate',
    /**@param {Client} client
     @param {{invite: Invite}}*/
    execute(client, { invite }) {

    }
}