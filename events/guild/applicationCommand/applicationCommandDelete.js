const { PinguEvent } = require('PinguPackage');

module.exports = new PinguEvent('applicationCommandDelete',
    async function setContent(client, embed, command) {
        
    },
    async function execute(client, command) {
        
    }
);